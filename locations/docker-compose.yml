version: "2"

services:
  app:
    image: golang:1.20-alpine
    depends_on:
      - kafka
    volumes:
      - ./:/usr/src/app

  kafka:
    image: docker.io/bitnami/kafka:3.4
    ports:
      - "9092:9092"
    volumes:
      - "kafka_data:/bitnami"
    environment:
      - ALLOW_PLAINTEXT_LISTENER=yes

volumes:
  kafka_data:
    driver: local
